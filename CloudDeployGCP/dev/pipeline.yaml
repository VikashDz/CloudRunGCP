apiVersion: deploy.cloud.google.com/v1
kind: DeliveryPipeline
metadata:
  name: react-app-pipeline
  labels: 
    app: react-app-pipeline
description: Cloud Deploy Pipeline for React App
serialPipeline:
  stages:
    - targetId: react-app-pipeline-dev
      profiles:
        - dev
      config:
        region: us-central1  
    
