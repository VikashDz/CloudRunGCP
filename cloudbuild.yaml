# steps:
# #  Build=
#   - name: 'gcr.io/cloud-builders/docker'
#     args: ['build', '-t', 'us-central1-docker.pkg.dev/gcp-praticals/react-app/web:latest', '.']
# #  Push
#   - name: 'gcr.io/cloud-builders/docker'
#     args: ['push', 'us-central1-docker.pkg.dev/gcp-praticals/react-app/web:latest']
# #  Deploy to Cloud Run=

# #  Trigger to Cloud Deploy
#   - name: 'google/cloud-sdk:latest'
#     id: Trigger Cloud Deploy
#     entrypoint: 'sh'
#     args:
#     - -xe
#     - -c
#     - |
#       gcloud config set deploy/region us-central1
#       gcloud deploy apply --file CloudDeployGCP/dev/pipeline.yaml
#       gcloud deploy apply --file CloudDeployGCP/dev/target.yaml
#       gcloud deploy releases create web-$SHORT_SHA \
#                           --delivery-pipeline=web \
#                           --skaffold-file=web.yaml


# ==================================================================================================================

steps:
  # Build
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'us-central1-docker.pkg.dev/gcp-praticals/react-app/web:latest', '.']

  # Push
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'us-central1-docker.pkg.dev/gcp-praticals/react-app/web:latest']

  # Deploy to Cloud Run
  # - name: 'gcr.io/cloud-builders/gcloud'
  #   args:
  #     - 'run'
  #     - 'deploy'
  #     - 'react-app'
  #     - '--image=us-central1-docker.pkg.dev/gcp-praticals/react-app/web:latest'
  #     - '--platform=managed'
  #     - '--region=us-central1'
  #     - '--allow-unauthenticated'

   # Trigger Cloud Deploy
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
      - 'deploy'
      - 'apply'
      - '--file=CloudDeployGCP/dev/pipeline.yaml'
      - '--file=CloudDeployGCP/dev/target.yaml'
      - '--region=us-central1'  # Specify the region as part of the deployment command
    

